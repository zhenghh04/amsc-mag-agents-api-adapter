# MAG ↔ OpenAI Agents API adapter — runtime deps
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
websockets>=12          # client for codex exec-server (real sandbox)

# --- optional, only for the SDK-contract validation / example runners ---
# openai==3.13.0        # provides openai.types.beta.agent_session[_event] used by validate_models.py
